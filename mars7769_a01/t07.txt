SELECT pubTitle
FROM pub
WHERE pubMemberId = (
    SELECT memberId
    FROM member
    WHERE memberForename = 'Mike'
      AND memberSurname = 'Bechtold'
)
AND pubPubType = (
    SELECT pubType
    FROM pubType
    WHERE pubTypeDesc = 'Journal Article'
)
ORDER BY pubTitle
