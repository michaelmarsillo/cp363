SELECT m.memberSurname, m.memberForename, pt.pubTypeDesc, p.pubTitle
FROM member m
JOIN pub p ON p.pubMemberId = m.memberId
JOIN pubType pt ON pt.pubType = p.pubPubType
WHERE m.memberInstitution LIKE '%McGill%'
ORDER BY m.memberSurname, m.memberForename
