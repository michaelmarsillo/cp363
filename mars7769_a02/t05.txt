SELECT m.memberSurname, m.memberForename, COUNT(p.pubId) AS numPublications
FROM member m
LEFT JOIN pub p ON p.pubMemberId = m.memberId
GROUP BY m.memberSurname, m.memberForename
ORDER BY m.memberSurname, m.memberForename
